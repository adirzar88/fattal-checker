# יו ספלאש — בדיקת זמינות אוטומטית ☁️

בודק כל 5 דקות אם יש חדרים פנויים ביו ספלאש ריזורט אילת ושולח מייל אוטומטי.

## הגדרה

פתח את הקובץ `config.py` ועדכן:
- `CHECK_IN` / `CHECK_OUT` — תאריכי החיפוש
- `ADULTS`, `CHILDREN`, `INFANTS` — הרכב האורחים
- `TO_EMAIL` — המייל לקבלת ההתראה
